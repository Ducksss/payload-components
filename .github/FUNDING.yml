# Funds maintenance only — Payload Components stays MIT and never gates components.
# The "Sponsor" button activates once GitHub Sponsors is enabled for this account.
github: [Ducksss]
# open_collective: payload-components
