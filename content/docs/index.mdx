---
title: Introduction
description: Payload Components is a Fumadocs-first documentation and registry project for installable Payload CMS blocks.
icon: Rocket
---

Payload Components is an open-source, community-first registry and CLI that installs Payload CMS blocks into your **Payload v3 + Next.js** project — **wired, not pasted**.

<Callout type="info" title="What makes a component more than a copy-paste block">
  A plain `shadcn add` only copies files. `payload-components add` also does the Payload
  wiring — it registers the block in your collection, maps the renderer, and
  regenerates types plus the admin import map — then lands the whole thing as a
  reviewable git diff.
</Callout>

The v2 app is intentionally not a Payload CMS site. It is a Fumadocs app with MDX documentation, static registry assets, and package tooling for validating installs into external Payload projects.

## Install your first component

```bash
npx payload-components add hero-basic
```

The CLI verifies project shape, installs the registry item, applies Payload layout fragments, runs post-install tasks, and records install state.

<Callout type="warn">
  Run this inside a supported **Payload v3 + Next.js App Router** project — not
  against this docs repo. [Installation](/docs/installation) lists the requirements
  the installer checks before it touches anything.
</Callout>

## Explore the docs

<Cards>
  <Card icon={<Layers />} title="Architecture" href="/docs/architecture">
    Why the Fumadocs site and the Payload component tooling stay on separate runtimes.
  </Card>
  <Card icon={<Terminal />} title="Installation" href="/docs/installation">
    Requirements, the `add` command, and exactly what the CLI does to a project.
  </Card>
  <Card icon={<Package />} title="Registry Contract" href="/docs/registry">
    How the public registry, manifests, and wrapper CLI fit together.
  </Card>
  <Card icon={<ShieldCheck />} title="Operations Runbook" href="/docs/operations">
    Release checks, support triage, security intake, rollback, and feedback routing.
  </Card>
  <Card icon={<GitPullRequest />} title="Contributing" href="/docs/contributing">
    The new-component checklist and how the catalog stays curated and verifiable.
  </Card>
</Cards>

## Start with these

These five are a solid first install — a slice of the catalog, not the whole of it:

<Cards>
  <Card icon={<LayoutPanelTop />} title="Hero Basic" href="/docs/components/hero-basic">
    Headline, description, one or two CTA links, and up to four proof badges. The golden component.
  </Card>
  <Card icon={<LayoutGrid />} title="Feature Grid Basic" href="/docs/components/feature-grid-basic">
    A text-first grid of repeatable feature cards with optional CTA wiring.
  </Card>
  <Card icon={<Layers />} title="Feature Split" href="/docs/components/feature-split">
    A two-column feature section: heading and CTA beside a stacked feature list.
  </Card>
  <Card icon={<Boxes />} title="Feature Bento" href="/docs/components/feature-bento">
    An asymmetric bento grid that leads with a featured cell.
  </Card>
  <Card icon={<Workflow />} title="Feature Steps" href="/docs/components/feature-steps">
    A numbered steps block for sequential, how-it-works flows.
  </Card>
</Cards>

Browse the full, always-current set rendered live in the [Component Catalog](/components).
