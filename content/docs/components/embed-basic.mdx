---
title: Embed Basic
description: A responsive, accessible sandboxed iframe block for approved HTTPS embeds — installed as a wired Payload block.
icon: MonitorPlay
---

<ComponentPreview slug="embed-basic" />

## Installation

<Tabs items={['Command', 'Manual']}>
  <Tab value="Command">
    ```bash
    npx payload-components add embed-basic
    ```
  </Tab>
  <Tab value="Manual">
    Copy the files straight from the registry, then wire the Payload fragments by hand:

    ```bash
    pnpm dlx shadcn@latest add https://www.payload-components.xyz/r/embed-basic.json
    ```
  </Tab>
</Tabs>

## What it installs

<ComponentWiring slug="embed-basic" />

## Content model

The block accepts approved HTTPS provider embed URLs — paste the provider's _embed_ URL (e.g.
`https://www.youtube.com/embed/VIDEO_ID`, not the watch URL). The iframe renders with a sandbox
and a narrowed permissions policy.

<TypeTable
  type={{
    url: {
      type: 'text',
      required: true,
      description: 'Approved HTTPS provider embed URL used as the iframe src.',
    },
    title: {
      type: 'text',
      required: true,
      description: 'Accessible title announced to screen readers for the embedded frame.',
    },
    aspectRatio: {
      type: 'select',
      required: true,
      description: 'Frame ratio: 16:9, 4:3, 1:1, or 21:9 (defaults to 16:9).',
    },
    caption: { type: 'text', description: 'Optional caption shown beneath the frame.' },
    allowFullscreen: {
      type: 'checkbox',
      description: 'Allow the embedded content to enter fullscreen (defaults to true).',
    },
  }}
/>

## Usage

<ComponentUsage slug="embed-basic" />

## Requirements

<ComponentRequirements slug="embed-basic" />
