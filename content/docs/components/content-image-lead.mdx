---
title: Content Image Lead
seoTitle: Image-led Content Block for Payload CMS
description: Install a typed Payload CMS content block with a full-width lead image, two-column copy, and CTA. The CLI wires it into your Pages layout and renderer.
icon: Image
---

<ComponentPreview slug="content-image-lead" />

## Installation

<Tabs items={['Command', 'Manual']}>
  <Tab value="Command">
    <RunnableCommand
      command="npx payload-components add content-image-lead"
      label="Copy the content-image-lead install command"
      trackInstall
      emphasis="primary"
    />
  </Tab>
  <Tab value="Manual">
    Copy the files straight from the registry, then wire the Payload fragments by hand:

    ```bash
    pnpm dlx shadcn@latest add https://www.payload-components.xyz/r/content-image-lead.json
    ```
  </Tab>
</Tabs>

## What it installs

<ComponentWiring slug="content-image-lead" />

## Content model

The `eyebrow`, `title`, and `paragraphs` fields come from the shared Content family base
(`src/blocks/shared/contentFields.ts`); the `image` upload and CTA `links` are specific to this
variant.

<TypeTable
  type={{
    eyebrow: { type: 'text', description: 'Optional small label shown above the title.' },
    title: { type: 'text', required: true, description: 'The section heading.' },
    paragraphs: {
      type: 'array',
      description: 'Body copy — one or more short paragraphs (min 1, max 4).',
    },
    image: { type: 'upload', description: 'Lead image (Media upload) shown full-width above the columns.' },
    links: { type: 'array', description: 'Optional CTA link (max 1).' },
  }}
/>

Each item in `paragraphs` carries:

<TypeTable
  type={{
    text: { type: 'textarea', required: true, description: 'A single paragraph of body copy.' },
  }}
/>

## Usage

<ComponentUsage slug="content-image-lead" />

## Requirements

<ComponentRequirements slug="content-image-lead" />

## In this family

<ComponentFamily slug="content-image-lead" />
