---
title: Content List
seoTitle: Content List Block for Payload CMS
description: Install a typed Payload CMS content-list block with a serif heading and labeled terms. The CLI wires it into your Pages layout, renderer, types, and import map.
icon: Columns2
---

<ComponentPreview slug="content-list" />

## Installation

<Tabs items={['Command', 'Manual']}>
  <Tab value="Command">
    <RunnableCommand
      command="npx payload-components add content-list"
      label="Copy the content-list install command"
      trackInstall
      emphasis="primary"
    />
  </Tab>
  <Tab value="Manual">
    Copy the files straight from the registry, then wire the Payload fragments by hand:

    ```bash
    pnpm dlx shadcn@latest add https://www.payload-components.xyz/r/content-list.json
    ```
  </Tab>
</Tabs>

## What it installs

<ComponentWiring slug="content-list" />

## Content model

This serif "list" variant uses its own fields (not the shared `contentFields` base): a heading beside
a list of labeled-term paragraphs.

<TypeTable
  type={{
    eyebrow: { type: 'text', description: 'Optional small label shown above the title.' },
    title: { type: 'text', required: true, description: 'The serif section heading.' },
    items: {
      type: 'array',
      required: true,
      description: 'The labeled-term list (min 2, max 6).',
    },
  }}
/>

Each item in `items` carries:

<TypeTable
  type={{
    term: { type: 'text', required: true, description: 'The lead-in term (rendered emphasized).' },
    description: { type: 'textarea', required: true, description: 'The term’s description.' },
  }}
/>

## Usage

<ComponentUsage slug="content-list" />

## Requirements

<ComponentRequirements slug="content-list" />

## In this family

<ComponentFamily slug="content-list" />
