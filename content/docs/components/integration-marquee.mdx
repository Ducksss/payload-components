---
title: Integration Marquee
description: Three auto-scrolling rows of integration logos around a featured brand mark, with progressive edge fades.
icon: GalleryHorizontalEnd
---

<ComponentPreview slug="integration-marquee" />

## Installation

<Tabs items={['Command', 'Manual']}>
  <Tab value="Command">
    ```bash
    npx payload-components add integration-marquee
    ```
  </Tab>
  <Tab value="Manual">
    Copy the files straight from the registry, then wire the Payload fragments by hand:

    ```bash
    pnpm dlx shadcn@latest add https://payload-components.xyz/r/integration-marquee.json
    ```
  </Tab>
</Tabs>

## What it installs

<ComponentWiring slug="integration-marquee" />

## Content model

`heading`, `subtext`, and `integrations` come from the shared `integrationFields` base. This variant
adds an optional `featuredLogo` over the center of the scrolling rows. The marquee respects
`prefers-reduced-motion`.

<TypeTable
  type={{
    heading: {
      type: 'text',
      required: true,
      description: 'Short heading shown under the rows.',
    },
    subtext: {
      type: 'textarea',
      description: 'Optional supporting paragraph under the heading.',
    },
    integrations: {
      type: 'array',
      required: true,
      description: 'Two to twelve integrations scrolled across three rows.',
    },
    featuredLogo: {
      type: 'upload',
      description: 'Optional center brand mark over the scrolling rows.',
    },
  }}
/>

Each item in `integrations` carries:

<TypeTable
  type={{
    logo: { type: 'upload', required: true, description: 'Logo image (Media upload).' },
    name: { type: 'text', required: true, description: 'Accessible name / alt text for the logo.' },
    description: { type: 'textarea', description: 'Optional blurb (unused by this variant).' },
    href: { type: 'text', description: 'Optional link (unused by this variant).' },
  }}
/>

## Usage

<ComponentUsage slug="integration-marquee" />

## Requirements

<ComponentRequirements slug="integration-marquee" />

## In this family

<ComponentFamily slug="integration-marquee" />
