---
title: Content Rows
description: A content section with an intro and a uniform stack of media-and-text rows.
icon: ListOrdered
---

<ComponentPreview slug="content-rows" />

## Installation

<Tabs items={['Command', 'Manual']}>
  <Tab value="Command">
    ```bash
    npx payload-components add content-rows
    ```
  </Tab>
  <Tab value="Manual">
    Copy the files straight from the registry, then wire the Payload fragments by hand:

    ```bash
    pnpm dlx shadcn@latest add https://www.payload-components.xyz/r/content-rows.json
    ```
  </Tab>
</Tabs>

## What it installs

<ComponentWiring slug="content-rows" />

## Content model

The `eyebrow`, `title`, and `paragraphs` fields come from the shared Content family base
(`src/blocks/shared/contentFields.ts`); the `rows` list is specific to this variant. Every row keeps
the media on the same side.

<TypeTable
  type={{
    eyebrow: { type: 'text', description: 'Optional small label shown above the title.' },
    title: { type: 'text', required: true, description: 'The section heading.' },
    paragraphs: {
      type: 'array',
      description: 'Intro copy — one or more short paragraphs (min 1, max 4).',
    },
    rows: {
      type: 'array',
      required: true,
      description: 'Two to four media-and-text rows.',
    },
  }}
/>

Each item in `rows` carries:

<TypeTable
  type={{
    image: { type: 'upload', description: 'Row media (Media upload).' },
    title: { type: 'text', required: true, description: 'Row heading.' },
    description: { type: 'textarea', required: true, description: 'Row body copy.' },
  }}
/>

## Usage

<ComponentUsage slug="content-rows" />

## Requirements

<ComponentRequirements slug="content-rows" />

## In this family

<ComponentFamily slug="content-rows" />
