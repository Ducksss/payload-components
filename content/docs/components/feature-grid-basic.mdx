---
title: Feature Grid Basic
description: A text-first grid of repeatable feature cards with optional CTA wiring — the base of the Feature family.
icon: LayoutGrid
---

<ComponentPreview slug="feature-grid-basic" />

## Installation

<Tabs items={['Command', 'Manual']}>
  <Tab value="Command">
    ```bash
    npx payload-components add feature-grid-basic
    ```
  </Tab>
  <Tab value="Manual">
    Copy the files straight from the registry, then wire the Payload fragments by hand:

    ```bash
    pnpm dlx shadcn@latest add https://payload-components.xyz/r/feature-grid-basic.json
    ```
  </Tab>
</Tabs>

## What it installs

<ComponentWiring slug="feature-grid-basic" />

## Content model

The first three fields come from the shared `featureFields` base; `items` and `links` are specific
to this variant.

<TypeTable
  type={{
    eyebrow: { type: 'text', description: 'Small label shown above the title.' },
    title: { type: 'text', required: true, description: 'The section heading.' },
    description: { type: 'textarea', description: 'Optional intro copy.' },
    items: {
      type: 'array',
      required: true,
      description: 'Three to six feature cards, each with a required title and description.',
    },
    links: { type: 'array', description: 'One optional CTA link.' },
  }}
/>

Each item in `items` carries:

<TypeTable
  type={{
    title: { type: 'text', required: true, description: 'Feature card heading.' },
    description: { type: 'textarea', required: true, description: 'Feature card body copy.' },
  }}
/>

## Usage

<ComponentUsage slug="feature-grid-basic" />

## Requirements

<ComponentRequirements slug="feature-grid-basic" />

## In this family

<ComponentFamily slug="feature-grid-basic" />
