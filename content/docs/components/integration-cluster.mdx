---
title: Integration Cluster
description: A centered cluster of integration logos around a featured brand mark, with a heading and CTA.
icon: Boxes
---

<ComponentPreview slug="integration-cluster" />

## Installation

<Tabs items={['Command', 'Manual']}>
  <Tab value="Command">
    ```bash
    npx payload-components add integration-cluster
    ```
  </Tab>
  <Tab value="Manual">
    Copy the files straight from the registry, then wire the Payload fragments by hand:

    ```bash
    pnpm dlx shadcn@latest add https://payload-components.xyz/r/integration-cluster.json
    ```
  </Tab>
</Tabs>

## What it installs

<ComponentWiring slug="integration-cluster" />

## Content model

`heading`, `subtext`, and `integrations` come from the shared `integrationFields` base. This variant
adds an optional `featuredLogo` center mark and a single CTA `links` group.

<TypeTable
  type={{
    heading: {
      type: 'text',
      required: true,
      description: 'Short heading shown under the logo cluster.',
    },
    subtext: {
      type: 'textarea',
      description: 'Optional supporting paragraph under the heading.',
    },
    integrations: {
      type: 'array',
      required: true,
      description: 'Two to twelve integrations, each an uploaded logo with a name.',
    },
    featuredLogo: {
      type: 'upload',
      description: 'Optional center brand mark the logos cluster around.',
    },
    links: {
      type: 'array',
      description: 'A single optional CTA link shown below the cluster.',
    },
  }}
/>

Each item in `integrations` carries:

<TypeTable
  type={{
    logo: { type: 'upload', required: true, description: 'Logo image (Media upload).' },
    name: { type: 'text', required: true, description: 'Accessible name / alt text for the logo.' },
    description: { type: 'textarea', description: 'Optional blurb (unused by this variant).' },
    href: { type: 'text', description: 'Optional link (unused by this variant).' },
  }}
/>

## Usage

<ComponentUsage slug="integration-cluster" />

## Requirements

<ComponentRequirements slug="integration-cluster" />

## In this family

<ComponentFamily slug="integration-cluster" />
