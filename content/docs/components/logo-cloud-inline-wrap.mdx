---
title: Logo Cloud Inline Wrap
description: A single wrapping row that keeps the label inline with editable trust logos, installed as a wired Payload block.
icon: StretchHorizontal
---

<ComponentPreview slug="logo-cloud-inline-wrap" />

## Installation

<Tabs items={['Command', 'Manual']}>
  <Tab value="Command">
    ```bash
    npx payload-components add logo-cloud-inline-wrap
    ```
  </Tab>
  <Tab value="Manual">
    Copy the files straight from the registry, then wire the Payload fragments by hand:

    ```bash
    pnpm dlx shadcn@latest add https://payload-components.xyz/r/logo-cloud-inline-wrap.json
    ```
  </Tab>
</Tabs>

## What it installs

<ComponentWiring slug="logo-cloud-inline-wrap" />

## Content model

Both fields come from the shared `logoCloudFields` base shared across the Logo Cloud family.

<TypeTable
  type={{
    heading: {
      type: 'text',
      required: true,
      description: 'Short label kept inline with the logos.',
    },
    logos: {
      type: 'array',
      required: true,
      description: 'Two to twelve logos, each an uploaded image with a name and optional link.',
    },
  }}
/>

Each item in `logos` carries:

<TypeTable
  type={{
    logo: { type: 'upload', required: true, description: 'Logo image (Media upload).' },
    name: { type: 'text', required: true, description: 'Accessible name / alt text for the logo.' },
    href: { type: 'text', description: 'Optional link the logo points to.' },
  }}
/>

## Usage

<ComponentUsage slug="logo-cloud-inline-wrap" />

## Requirements

<ComponentRequirements slug="logo-cloud-inline-wrap" />

## In this family

<ComponentFamily slug="logo-cloud-inline-wrap" />
