---
title: Integration Split
description: A two-column section pairing a featured-mark logo cluster with a heading, subtext, and CTA.
icon: Columns2
---

<ComponentPreview slug="integration-split" />

## Installation

<Tabs items={['Command', 'Manual']}>
  <Tab value="Command">
    ```bash
    npx payload-components add integration-split
    ```
  </Tab>
  <Tab value="Manual">
    Copy the files straight from the registry, then wire the Payload fragments by hand:

    ```bash
    pnpm dlx shadcn@latest add https://www.payload-components.xyz/r/integration-split.json
    ```
  </Tab>
</Tabs>

## What it installs

<ComponentWiring slug="integration-split" />

## Content model

`heading`, `subtext`, and `integrations` come from the shared `integrationFields` base. This variant
adds an optional `featuredLogo` center mark and a single CTA `links` group beside the cluster.

<TypeTable
  type={{
    heading: {
      type: 'text',
      required: true,
      description: 'Short heading shown beside the logo cluster.',
    },
    subtext: {
      type: 'textarea',
      description: 'Optional supporting paragraph under the heading.',
    },
    integrations: {
      type: 'array',
      required: true,
      description: 'Two to twelve integrations, each an uploaded logo with a name.',
    },
    featuredLogo: {
      type: 'upload',
      description: 'Optional center brand mark the logos cluster around.',
    },
    links: {
      type: 'array',
      description: 'A single optional CTA link shown beside the cluster.',
    },
  }}
/>

Each item in `integrations` carries:

<TypeTable
  type={{
    logo: { type: 'upload', required: true, description: 'Logo image (Media upload).' },
    name: { type: 'text', required: true, description: 'Accessible name / alt text for the logo.' },
    description: { type: 'textarea', description: 'Optional blurb (unused by this variant).' },
    href: { type: 'text', description: 'Optional link (unused by this variant).' },
  }}
/>

## Usage

<ComponentUsage slug="integration-split" />

## Requirements

<ComponentRequirements slug="integration-split" />

## In this family

<ComponentFamily slug="integration-split" />
