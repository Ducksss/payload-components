# CLAUDE.md

Agent guidance for this repo lives in **[AGENTS.md](./AGENTS.md)** — read it first.

It covers what Payload Components is, the community-first positioning, the site-vs-component-distribution mental model, the repo map, the core flows (the `payload-components add` pipeline, registry generation, demo twins), where to change things, the test contract that must stay green, the release gate, and the branch/release flow.

`AGENTS.md` is the single source of truth; this file only points to it so guidance never drifts between the two.
